% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_scenarios.R
\name{chlaa_forecast_scenarios_from_fit}
\alias{chlaa_forecast_scenarios_from_fit}
\title{Forecast multiple scenarios from a fitted posterior}
\usage{
chlaa_forecast_scenarios_from_fit(
  fit,
  pars = NULL,
  scenarios = NULL,
  baseline_name = "baseline",
  time = NULL,
  vars = c("inc_symptoms", "cum_symptoms", "cum_deaths"),
  include_cases = TRUE,
  obs_model = c("nbinom", "mean"),
  obs_interval = NULL,
  quantiles = c(0.025, 0.25, 0.5, 0.75, 0.975),
  n_draws = 100,
  burnin = 0.5,
  thin = 1,
  seed = 1,
  dt = 0.25,
  n_particles = 1,
  n_threads = 1,
  deterministic = FALSE,
  include_baseline_in_scenarios = TRUE
)
}
\arguments{
\item{fit}{A `chlaa_fit` object.}

\item{pars}{Baseline parameter list. If NULL uses `attr(fit, "start_pars")` else `chlaa_parameters()`.}

\item{scenarios}{Scenarios to run (list of `chlaa_scenario`, named list of modify lists, or a grid data.frame).}

\item{baseline_name}{Baseline scenario name (modify list may be empty).}

\item{time}{Simulation times. If NULL uses `fit` data times.}

\item{vars}{Model variables to summarise.}

\item{include_cases}{Include predicted observed cases variable "cases".}

\item{obs_model}{One of "nbinom" or "mean".}

\item{obs_interval}{Observation interval in days for generated observed
cases. If NULL, uses `attr(fit, "obs_interval")`, falling back to 1.}

\item{quantiles}{Quantiles to compute.}

\item{n_draws}{Number of posterior draws to use.}

\item{burnin}{Burn-in proportion or integer.}

\item{thin}{Thinning interval.}

\item{seed}{Seed.}

\item{dt}{Model time step.}

\item{n_particles}{Particles per draw.}

\item{n_threads}{Threads for dust2.}

\item{deterministic}{Deterministic process model toggle (if supported).}

\item{include_baseline_in_scenarios}{If TRUE, ensures baseline is included even if not passed.}
}
\value{
A tidy data.frame with columns: scenario, type, time, variable, mean, quantiles, n_samples.
}
\description{
Selects posterior iterations once (shared across scenarios), simulates baseline and each scenario
using the same draw indices and RNG seed streams, and returns both absolute forecasts and paired
differences vs baseline.
}
