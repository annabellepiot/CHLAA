% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{chlaa_forecast_from_fit}
\alias{chlaa_forecast_from_fit}
\title{Forecast summary from a fitted posterior}
\usage{
chlaa_forecast_from_fit(
  fit,
  pars = NULL,
  time = NULL,
  vars = c("inc_symptoms", "cum_symptoms", "cum_deaths"),
  include_cases = TRUE,
  obs_model = c("nbinom", "mean"),
  quantiles = c(0.025, 0.25, 0.5, 0.75, 0.975),
  n_draws = 100,
  burnin = 0.5,
  thin = 1,
  seed = 1,
  dt = 0.25,
  n_particles = 1,
  n_threads = 1,
  deterministic = FALSE,
  obs_interval = NULL,
  modify = NULL
)
}
\arguments{
\item{fit}{A `chlaa_fit` object (or compatible).}

\item{pars}{Baseline parameter list. If NULL, uses `attr(fit, "start_pars")`, otherwise `chlaa_parameters()`.}

\item{time}{Vector of times to simulate.}

\item{vars}{Character vector of model variables to summarise.}

\item{include_cases}{Logical; include predicted observed cases as variable "cases".}

\item{obs_model}{One of "nbinom" or "mean".}

\item{quantiles}{Numeric vector of quantiles to return.}

\item{n_draws}{Number of posterior draws to use.}

\item{burnin}{Burn-in proportion or integer.}

\item{thin}{Thinning interval.}

\item{seed}{Seed.}

\item{dt}{Model time step.}

\item{n_particles}{Particles per posterior draw.}

\item{n_threads}{Threads for dust2.}

\item{deterministic}{Run the process model deterministically (if supported).}

\item{obs_interval}{Observation interval in days for generated observed
cases. If NULL, uses `attr(fit, "obs_interval")`, falling back to 1.}

\item{modify}{Optional named list of parameter modifications applied after each draw update.}
}
\value{
A tidy data.frame with columns: time, variable, mean, quantiles, n_samples.
}
\description{
Runs the model for multiple posterior draws and returns time-by-time summaries
(mean and quantiles) for selected variables.
}
\details{
If `include_cases = TRUE`, this also generates a predictive distribution for observed
cases using the observation model:
  mu = reporting_rate * observed incidence
where observed incidence is daily `inc_symptoms` for `obs_interval = 1`, or
weekly `inc_symptoms_weekly` for `obs_interval = 7`.
and either:
- `obs_model = "nbinom"`: sample Negative Binomial noise
- `obs_model = "mean"`: use mu directly
}
